<h1> edit yor post </h1>

<%= form_for [@post] do |p| %>
	<%= p.label :title %>
	<%= p.text_field :title %>
	<br>

	<%= p.label :content %> <br>
	<%= p.text_area :content %>
	<br>

	<%= p.submit 'save' %>
	<%= link_to 'cancel', post_path(@post) %>
<% end %>